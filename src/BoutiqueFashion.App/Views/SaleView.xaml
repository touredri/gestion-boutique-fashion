<UserControl x:Class="BoutiqueFashion.App.Views.SaleView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:domain="clr-namespace:BoutiqueFashion.Domain;assembly=BoutiqueFashion.Domain" xmlns:controls="clr-namespace:BoutiqueFashion.App.Controls">
  <Grid Margin="14">
    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="430"/></Grid.ColumnDefinitions>
    <Grid Margin="0,0,8,0">
      <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
      <Border Style="{StaticResource Card}" Margin="0,0,0,8" Padding="12">
        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <StackPanel><TextBlock Text="Rechercher ou scanner (nom, SKU, code-barres)" Style="{StaticResource FieldLabel}" Margin="4,0,4,1"/><TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" controls:Placeholder.Text="Nom, SKU ou code-barres" controls:TouchKeyboard.Suppress="True" AutomationProperties.Name="Recherche produit"/></StackPanel>
          <Button Grid.Column="1" Content="Rechercher" Command="{Binding SearchProductsCommand}" Style="{StaticResource PrimaryButton}" VerticalAlignment="Bottom"/>
        </Grid>
      </Border>
      <ListBox Grid.Row="1" ItemsSource="{Binding Products}" ScrollViewer.VerticalScrollBarVisibility="Auto">
        <ListBox.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ListBox.ItemsPanel>
        <ListBox.ItemTemplate><DataTemplate>
          <Button Width="168" Height="178" Margin="4" Padding="12" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Command="{Binding DataContext.AddCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}">
            <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
              <Border Height="78" CornerRadius="8" Background="{StaticResource Ivory}" BorderBrush="{StaticResource Line}" BorderThickness="1" Margin="0,0,0,5" HorizontalAlignment="Stretch">
                <Image Source="{Binding PrimaryImagePath, Converter={StaticResource ImagePathConverter}}" Stretch="UniformToFill" Margin="3">
                  <Image.Style><Style TargetType="Image"><Setter Property="Visibility" Value="Visible"/><Style.Triggers><Trigger Property="Source" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed"/></Trigger></Style.Triggers></Style></Image.Style>
                </Image>
              </Border>
              <StackPanel Grid.Row="1"><TextBlock Text="{Binding Product.Name}" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" MaxHeight="38"/><TextBlock Text="{Binding Sku}" Foreground="{StaticResource Muted}" FontSize="10" Margin="0,1,0,0"/></StackPanel>
              <Grid Grid.Row="2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding PriceXof, StringFormat={}{0:N0} FCFA}" Foreground="{StaticResource Terracotta}" FontSize="14" Style="{StaticResource Money}"/><TextBlock Grid.Column="1" Text="{Binding QuantityOnHand, StringFormat=Stock : {0:0.###}}" Foreground="{StaticResource Muted}" FontSize="10" Margin="0,3,0,0"/></Grid>
            </Grid>
          </Button>
        </DataTemplate></ListBox.ItemTemplate>
      </ListBox>
    </Grid>

    <Border Grid.Column="1" Style="{StaticResource RaisedCard}" Margin="0" Padding="0">
      <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*" MinHeight="90"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid Margin="16,10,14,6"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <StackPanel><TextBlock Text="PANIER EN COURS" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding PayableXof, StringFormat={}{0:N0} FCFA}" FontSize="24" Foreground="{StaticResource Terracotta}" Style="{StaticResource Money}"/></StackPanel>
          <TextBlock Grid.Column="1" Text="{Binding Status}" Style="{StaticResource HelperText}" MaxWidth="180" TextWrapping="Wrap" VerticalAlignment="Center" AutomationProperties.LiveSetting="Polite"/>
        </Grid>
        <ListBox Grid.Row="1" ItemsSource="{Binding Cart}" Margin="10,0" ScrollViewer.VerticalScrollBarVisibility="Auto">
          <ListBox.ItemTemplate><DataTemplate><Border BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="2,4"><StackPanel>
            <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="42"/><ColumnDefinition Width="84"/><ColumnDefinition Width="42"/><ColumnDefinition Width="86"/><ColumnDefinition Width="42"/></Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Label}" TextWrapping="Wrap" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <Button Grid.Column="1" Content="−" MinWidth="0" Width="38" MinHeight="42" Style="{StaticResource QuietButton}" Command="{Binding DataContext.DecrementCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" AutomationProperties.Name="Diminuer la quantité"/>
            <controls:KeypadField Grid.Column="2" Value="{Binding Quantity}" Title="Quantité" Margin="2,0"/>
            <Button Grid.Column="3" Content="+" MinWidth="0" Width="38" MinHeight="42" Style="{StaticResource QuietButton}" Command="{Binding DataContext.IncrementCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" AutomationProperties.Name="Augmenter la quantité"/>
            <TextBlock Grid.Column="4" Text="{Binding TotalXof, StringFormat={}{0:N0}}" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource Money}"/>
            <Button Grid.Column="5" Content="×" MinWidth="0" Width="38" MinHeight="42" ToolTip="Retirer l'article" AutomationProperties.Name="Retirer l'article" Style="{StaticResource QuietButton}" Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}"/>
            </Grid>
            <Grid Margin="0,2,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="104"/><ColumnDefinition Width="96"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
              <TextBlock Text="Remise" Foreground="{StaticResource Muted}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" Margin="4,0,6,0"/>
              <ComboBox Grid.Column="1" ItemsSource="{Binding DiscountKindLabels}" SelectedItem="{Binding DiscountKindLabel}" MinHeight="40" AutomationProperties.Name="Type de remise article"/>
              <controls:KeypadField Grid.Column="2" Value="{Binding DiscountValue}" Title="Valeur de remise" Margin="2,0"/>
              <TextBlock Grid.Column="3" Text="{Binding DiscountAmountXof, StringFormat=- {0:N0} FCFA}" Foreground="{StaticResource Danger}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </Grid>
          </StackPanel></Border></DataTemplate></ListBox.ItemTemplate>
        </ListBox>
        <TabControl Grid.Row="2" Height="344" Margin="8,6,8,8" Padding="0">
          <TabItem Header="Encaissement">
            <Grid Margin="6,2">
              <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
              <WrapPanel>
                <Button Content="Espèces" MinWidth="118" MinHeight="44" Command="{Binding QuickPayCommand}" CommandParameter="{x:Static domain:PaymentMode.Cash}"/>
                <Button Content="Orange Money" MinWidth="118" MinHeight="44" Command="{Binding QuickPayCommand}" CommandParameter="{x:Static domain:PaymentMode.OrangeMoney}"/>
                <Button Content="Moov Money" MinWidth="118" MinHeight="44" Command="{Binding QuickPayCommand}" CommandParameter="{x:Static domain:PaymentMode.MoovMoney}"/>
                <Button Content="Wave" MinWidth="118" MinHeight="44" Command="{Binding QuickPayCommand}" CommandParameter="{x:Static domain:PaymentMode.Wave}"/>
                <Button Content="Carte" MinWidth="118" MinHeight="44" Command="{Binding QuickPayCommand}" CommandParameter="{x:Static domain:PaymentMode.Card}"/>
                <Button Content="Crédit" MinWidth="118" MinHeight="44" Command="{Binding QuickPayCommand}" CommandParameter="{x:Static domain:PaymentMode.Credit}" Style="{StaticResource DangerButton}"/>
              </WrapPanel>
              <Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <controls:KeypadField Value="{Binding DiscountPercent}" Title="Remise %" Suffix="%" Margin="0,0,4,0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                  <Button Content="+7 j" MinWidth="44" MinHeight="40" Padding="6,4" Command="{Binding SetCreditDueCommand}" CommandParameter="7"/>
                  <Button Content="+15 j" MinWidth="44" MinHeight="40" Padding="6,4" Command="{Binding SetCreditDueCommand}" CommandParameter="15"/>
                  <Button Content="+30 j" MinWidth="44" MinHeight="40" Padding="6,4" Command="{Binding SetCreditDueCommand}" CommandParameter="30"/>
                  <Button Content="+60 j" MinWidth="44" MinHeight="40" Padding="6,4" Command="{Binding SetCreditDueCommand}" CommandParameter="60"/>
                </StackPanel>
                <TextBlock Grid.Column="2" Text="{Binding CreditDueDate}" Foreground="{StaticResource Muted}" FontSize="11" VerticalAlignment="Center" Margin="4,0,2,0"/>
              </Grid>
              <StackPanel Grid.Row="2">
                <ItemsControl ItemsSource="{Binding Payments}"><ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,1"><Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="110"/><ColumnDefinition Width="*"/><ColumnDefinition Width="44"/></Grid.ColumnDefinitions><ComboBox ItemsSource="{Binding Modes}" SelectedItem="{Binding Mode}" MinHeight="40" AutomationProperties.Name="Mode de paiement"><ComboBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding Converter={StaticResource EnumFr}}"/></DataTemplate></ComboBox.ItemTemplate></ComboBox><controls:KeypadField Grid.Column="1" Value="{Binding AmountXof}" Title="Montant reçu" Margin="2,0"/><TextBox Grid.Column="2" Text="{Binding Reference}" controls:Placeholder.Text="Référence (optionnel)" AutomationProperties.Name="Référence du paiement"/><Button Grid.Column="3" Content="×" MinWidth="0" Width="38" MinHeight="40" ToolTip="Retirer ce paiement" AutomationProperties.Name="Retirer ce paiement" Style="{StaticResource QuietButton}" Command="{Binding DataContext.RemovePaymentCommand,RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                  <Button Content="+ Paiement mixte" Command="{Binding AddPaymentCommand}" Style="{StaticResource QuietButton}" HorizontalAlignment="Left"/>
                  <TextBlock Grid.Column="2" Text="{Binding ChangePreview, StringFormat=Monnaie à rendre : {0:N0} FCFA}" FontWeight="SemiBold" Foreground="{StaticResource Success}" VerticalAlignment="Center"/>
                </Grid>
              </StackPanel>
              <ComboBox Grid.Row="3" ItemsSource="{Binding Printers}" SelectedItem="{Binding SelectedPrinter}" DisplayMemberPath="Name" MinHeight="42" AutomationProperties.Name="Imprimante"/>
              <Button Grid.Row="4" Content="Encaisser et imprimer" Command="{Binding CompleteCommand}" Style="{StaticResource HeroButton}" MinHeight="50" Margin="4,2,4,2"/>
            </Grid>
          </TabItem>
          <TabItem Header="Client">
            <Grid Margin="8,6">
              <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
              <TextBox Text="{Binding CustomerSearch, UpdateSourceTrigger=PropertyChanged}" controls:Placeholder.Text="Nom ou téléphone" AutomationProperties.Name="Recherche client"/>
              <ListBox Grid.Row="1" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate><DataTemplate><Grid Margin="2,4"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Name}" FontWeight="SemiBold"/><TextBlock Grid.Column="1" Text="{Binding Phone}" Foreground="{StaticResource Muted}" Margin="8,0,0,0"/></Grid></DataTemplate></ListBox.ItemTemplate>
              </ListBox>
              <TextBlock Grid.Row="2" Margin="4,4" Style="{StaticResource HelperText}"><Run Text="Client sélectionné : "/><Run Text="{Binding SelectedCustomer.Name, Mode=OneWay, FallbackValue=aucun (vente comptoir)}"/></TextBlock>
            </Grid>
          </TabItem>
          <TabItem Header="Clôture caisse">
            <StackPanel Margin="8,6">
              <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <controls:KeypadField Value="{Binding OpeningFloat}" Title="Fond initial" Suffix="FCFA"/>
                <Button Grid.Column="1" Content="Ouvrir la caisse" Command="{Binding OpenCashCommand}" VerticalAlignment="Center"/>
              </Grid>
              <controls:KeypadField Value="{Binding CountedCash}" Title="Espèces comptées" Suffix="FCFA"/>
              <TextBox Text="{Binding CashDifferenceReason}" ToolTip="Motif de l'écart (obligatoire si écart)"/>
              <Button Content="Clôturer la caisse" Command="{Binding CloseCashCommand}" Style="{StaticResource DangerButton}" Margin="4,4,4,2"/>
            </StackPanel>
          </TabItem>
        </TabControl>
      </Grid>
    </Border>
  </Grid>
</UserControl>
